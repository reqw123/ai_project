# ─── Python bytecode / build ─────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.eggs/

# ─── PyInstaller 中間產物 ─────────────────────────────────
*.spec
paper/build/
paper/dist/

# ─── 測試 / 型別 / lint 快取 ──────────────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# ─── 編輯器 / OS ──────────────────────────────────────────
*.swp
*~
.DS_Store
Thumbs.db

# ─── 執行期輸出目錄（config.py ModelPaths 預設 ./logs、./output）─
# 已追蹤的 paper/output/*.csv 不受影響；此規則只避免新增執行期日誌。
/paper/logs/

# 貓咪身分辨識 CNN（tools/cat_identity/）的所有產物——資料量大，不進版控：
#   dataset/  裁切圖+原圖資料集（1_build_dataset.py）
#   infer/    影片端逐幀辨識輸出（3_infer_video.py）
#   trimmed/  剪掉沒貓片段的影片（0_trim_videos.py）
/paper/cat_monitoring_system/tools/train_data/cat_identity/

# 注意：identity_models/（tools/cat_identity/2_train.py 訓練出的身分 CNN 權重 +
# 每次訓練產物）刻意「不」忽略——比照 yolo_models / stgcn_models（同層）一起進版控。

# runtime_settings.previous.json 是 settings_manager 存檔時自動產生的備份，
# 且 paper/tests/test_settings_manager.py 的一致性測試會在 cleanup 時刪除它
# （已知的測試污染真實檔案問題，見 TODO M-10）——不進版控。
paper/runtime_settings.previous.json

# paper/local_state/：每台機器 / 使用者各自的本機狀態（換電腦就該重來），
# 跟進版控的系統設定分開——整個資料夾不進版控，只留 README.md 讓慣例被看到。
# 目前內容：gui_state.json（設定視窗 GUI 偏好，見 settings_gui/ui_state.py）。
paper/local_state/*
!paper/local_state/README.md
paper/.gui_state.json  # 舊位置（2026-08-31 前），殘留檔一併忽略

# ─── 執行期資料：每台機器各自累積，不進版控 ─────────────────────────
# 2026-09-03：改為不追蹤。原本刻意追蹤是為了「換機器路徑不用改」，但兩台裝置
# 都跑監測時，這些檔會一直變動 + 二進位 SQLite 無法 merge → 每次 git pull 都
# 衝突。改成各機器本機保有自己的一份（folder 由程式自動建立），git pull 只動
# 程式碼。要合併兩台的多天歷史見 docs（daily_history 用 INSERT OR IGNORE 合
# 不重疊的日期）。
paper/baseline_data/daily_history.db
paper/baseline_data/tracker_state.json
paper/baseline_data/global.json          # Node-RED 每個 instance 各一份
paper/cat_monitoring_log.csv
paper/behavior_segments_log.csv
/paper/output/

# runtime_settings.current.json：機器專屬設定（影像來源、絕對路徑…）。刪掉或
# 不存在時 config.py 會用「相對專案根目錄」的可攜預設；新機器用設定視窗重設。
# 共用的設定「決策」放 default_runtime_settings.json（仍追蹤）當參考。
paper/runtime_settings.current.json

# 注意：以下刻意「不」忽略——模型權重跟著程式碼一起進版控：
#   yolo_models/*.pt, stgcn_models/**, identity_models/**（移出需 history rewrite，見 TODO L-6）
