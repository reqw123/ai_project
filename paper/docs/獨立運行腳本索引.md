# 📑 獨立運行腳本索引

> [!NOTE]
> **本文件用途**
>
> 索引 `cat_pose/` 與 `paper/` 底下**目前實際存在**、設計成 `python xxx.py` 直接執行的獨立腳本（不是被其他模組 import 的函式庫），逐一列出功能、使用方法、路徑來源。這是操作手冊性質的索引，不是稽核報告——「哪些腳本功能重複、建議保留哪一支」的比對分析請見對話中的整理紀錄，這裡不重複。
>
> **跟其他文件的分工**：
> - `paper/cat_monitoring_system/tools/` 的簡短用途說明也出現在 `paper/docs/0_AI_專案導覽地圖.md` 第 3.6 節（一句話等級）——本文件補齊「怎麼跑」的操作細節，兩份互補。
> - `paper/config.py`、`paper/settings_window.py` 這兩支雖然也是可直接執行的腳本，完整欄位/操作說明已有專文 `paper/docs/設定視窗欄位對照表.md`，本文件只放摘要 + 連結，不重複整份內容。
>
> 校對時間：2026-08-15，以當下實際檔案為準（幾支已在近期整理中刪除/搬移/精簡，見各節備註）。

> [!IMPORTANT]
> **本文件第 5、6 節已跟 `settings_window.py` 程式綁定，不是純參考文件**
>
> `settings_window.py` 的「獨立腳本工具」下拉選單／常駐說明卡片，會在每次開啟設定視窗時即時解析本文件第 5 節（`paper/cat_monitoring_system/tools/`）與第 6 節（`.../train_data/`）的表格，抓「功能」欄位文字顯示給使用者看（實作見 `settings_window.py` 的 `_load_tool_script_descriptions()`）。**編輯這兩節時請遵守以下格式，否則對應腳本的說明會悄悄消失（不會噴錯，GUI 只會顯示「找不到功能說明」）：**
>
> 1. **表格列格式固定**：`| \`檔名.py\` | 功能說明文字 | 使用方法 |`——開頭是 `|`，接著檔名用單一反引號 `` ` `` 包住，再一個 `|`，然後是功能說明。解析用的是逐行 regex（`^\|\s*` `` `([^`]+)` `` `\s*\|\s*([^|]+?)\s*\|`），檔名反引號跟前後兩個 `|` 缺一都會讓那一列被跳過、視為沒有這支腳本的說明。
> 2. **功能說明欄不能包含 `|` 字元**：regex 是照 `|` 切欄位的，說明文字裡如果出現直線符號 `|`，會被誤判成欄位分隔線，說明文字被腰斬。要表達「或」的意思請用「／」或「或」，不要用 `|`。
> 3. **第 5 節的檔名＝相對於 `tools/` 的路徑**（例如 `eval_pose_compare.py`），**第 6 節（`train_data/` 子資料夾）的檔名不要加 `train_data/` 前綴**（直接寫 `0_dataset_collect.py`）——程式讀第 6 節時會自動幫每一列補上 `train_data/` 前綴；如果表格裡自己也手動加了前綴，會變成 `train_data/train_data/0_dataset_collect.py`，跟磁碟上的真實路徑對不起來，說明照樣會消失。
> 4. **檔名要跟磁碟上的實際檔名逐字元一致（含大小寫）**：程式內部是用 Python 字典查找比對，就算 Windows 檔案系統不分大小寫，字典比對還是精確比對，`Eval_Pose.py` 對不上實際檔名 `eval_pose.py`。
> 5. **章節標題文字不能改**：程式是用 `## 5. paper/cat_monitoring_system/tools/` 與 `## 6. paper/cat_monitoring_system/tools/train_data/` 這兩個確切字串（含編號）找到對應章節的表格範圍，改了標題文字或編號，程式就找不到該章節，整節說明會全部讀不到。
> 6. **在 `tools/`／`train_data/` 底下多列一支實際不存在的腳本不會出錯**——那一列會被解析進記憶體，但因為下拉選單是照磁碟上實際掃到的檔案建立的（不是照這份表格建立），不存在的腳本不會出現在清單裡，純粹是白寫的一筆資料，建議清掉避免文件本身失真，但不影響 GUI 運作。
> 7. 反過來，**`tools/`／`train_data/` 底下新增腳本但沒有補進這兩節的表格**：GUI 一樣會抓到新腳本、正常出現在下拉選單，只是說明卡片會顯示「找不到功能說明」，不影響能不能選取/執行。

---

## 目錄

1. [cat_pose/ 主體](#1-cat_pose-主體)
2. [cat_pose/自動標註工具/](#2-cat_pose自動標註工具)
3. [cat_pose/tello_drone_archive/ ⚠️ 已停用](#3-cat_posetello_drone_archive-️-已停用)
4. [paper/ 頂層](#4-paper-頂層)
5. [paper/cat_monitoring_system/tools/](#5-papercat_monitoring_systemtools)
6. [paper/cat_monitoring_system/tools/train_data/](#6-papercat_monitoring_systemtoolstrain_data)
7. [paper/_tools/](#7-paper_tools)
8. [paper/node_red_tests/](#8-papernode_red_tests)

---

## 1. cat_pose/ 主體

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `cat_pose/0_compare_two_models_images.py` | 比較兩個 YOLO Pose 模型在同一批圖片上的關鍵點偏移，輸出並排比較圖 + 偏移超標的圖片另存一份（offset_dataset） | 改頂部 `MODELS`（兩個模型路徑+imgsz）、`INPUT_DIR`（來源圖片資料夾），執行 `python 0_compare_two_models_images.py`；先清空舊輸出再跑。輸出到 `C:\cat_pose\cat_Compare\`（`compare_output/` 並排圖、`offset_dataset/` 偏移圖、`_resized_640_cache/` 640px 工作副本） |
| `cat_pose/0_video_pose_viewer.py` | 互動式多模型影片骨架檢視器，YOLO-only，可切換模型/影片、跳幀、裁切、方向鍵微調裁切框 | 改頂部 `MODEL_LIST`（最多 3 個模型路徑）、`VIDEO_SOURCES`（影片檔或資料夾列表），執行後看終端印出的完整快捷鍵表（`1/2/3` 切模型、`6~l` 切影片、`SPACE` 播放/暫停、`A/D` 逐幀、`P` 循環裁切模式、`O/I/U` 切換 overlay、`Q` 結束） |
| `cat_pose/111.py` | ffmpeg 批次等比縮放+補黑邊到固定 1920×1080 | 改頂部 `INPUT_FOLDER`，執行 `python 111.py`；需系統已安裝 ffmpeg/ffprobe。輸出到 `<INPUT_FOLDER>/output_1920x1080/` |
| `cat_pose/augmentation_yolo_pose.py` | YOLO Pose 資料增強，**同步變換關鍵點/BBox 座標**（跟已刪除的 `augmentation.py` 差異：後者只動影像、不處理標註，會讓座標對不上圖） | 改頂部 `INPUT_IMAGES_DIR`/`INPUT_LABELS_DIR`（來源 images+labels 成對資料）、`OUTPUT_IMAGES_DIR`/`OUTPUT_LABELS_DIR`，以及 `USE_HORIZONTAL_FLIP`/`USE_ROTATION`/`USE_CROP` 等增強開關，執行 `python augmentation_yolo_pose.py` |
| `cat_pose/dedup_images.py` | 依 MD5 雜湊找出重複圖片，可複製去重或就地刪除 | 改 `MODE`（1=複製不重複圖到新資料夾／2=就地刪除重複檔，保留最早出現者）、`DRY_RUN`（True 僅預覽）、`SOURCE_FOLDERS`，執行 `python dedup_images.py`。MODE=1 輸出到 `OUTPUT_FOLDER` |
| `cat_pose/dedup_videos.py` | 依檔案大小分組+MD5 找出**完全相同**的重複影片檔（逐位元組一致，非內容相似） | `python dedup_videos.py <資料夾1> [資料夾2] ...`（命令列帶路徑），或不帶參數改用頂部 `SOURCE_FOLDERS` 預設值。列出重複清單後終端詢問是否刪除，輸入 `y` 才真的刪，其餘任何輸入取消 |
| `cat_pose/eda_motion_anomaly.py` | EDA 探索腳本：對單支影片逐幀算關鍵點位移/EMA/加速度等異常訊號，輸出逐幀 CSV | 改頂部 `MODEL_PATH`/`VIDEO_PATH`/`EMA_ALPHA` 等參數，執行 `python eda_motion_anomaly.py`。輸出同目錄 `eda_keypoint_jitter.csv`（下一支腳本讀取用） |
| `cat_pose/eda_motion_score.py` | 讀取 `eda_motion_anomaly.py` 產生的 CSV，畫出動作異常分數圖表 | 需先跑過 `eda_motion_anomaly.py` 產生 `eda_keypoint_jitter.csv`；頂部 `SOURCE_VIDEO_PATH`/`SOURCE_MODEL_PATH` 必須跟產生 CSV 時完全一致（否則 frame 編號對不上畫面），執行 `python eda_motion_score.py` |
| `cat_pose/eda_realtime_anomaly_viewer.py` | 即時播放影片時同步顯示關鍵點抖動異常（MILD 黃色／SEVERE 紅色兩級門檻），互動視窗版 | 改頂部 `MODEL_PATH`/`VIDEO_PATH`/`MILD_THRES`/`SEVERE_THRES`，執行 `python eda_realtime_anomaly_viewer.py`；播放中按 `q` 結束、`+`/`-` 縮放 |
| `cat_pose/pose_quality_assessor.py` | 貓咪姿態標記品質自動稽核（Teacher-Student／Embedding 離群／Loss Tracking／Augmentation Consistency 四訊號加權評分），找出可疑的標記錯誤 | CLI 工具：`python pose_quality_assessor.py --model <teacher.pt> --data_dir <含images/labels的資料夾> --output report.json`，另有 `--student`（第二模型）、`--visualize`、`--disable-*`（停用個別檢測方法）、`--clear-output`/`--auto-naming`（輸出目錄處理策略）等參數，`--help` 可看完整範例 |
| `cat_pose/pose_single_image_test.py` | 單張圖片快速推論檢視，跳出 tkinter 檔案選擇對話框挑圖 | 執行 `python pose_single_image_test.py`，跳出視窗後選圖片即可；模型路徑寫死在頂部 `model = YOLO(...)`，需要換模型直接改那一行 |
| `cat_pose/tail_bend_detect.py` | 貓咪尾巴彎曲/伸直專用視覺化分析（依 tail_base→tail_mid→tail_tip 夾角分 STRAIGHT/SLIGHT_BEND/BENT 三級） | 沿用 `0_video_pose_viewer.py` 同款操作介面（`SPACE` 播放/暫停、`A/D` 逐幀、`1/2/3` 切模型、`6~l` 切影片、`Q` 結束），改頂部模型/影片清單後執行 `python tail_bend_detect.py` |
| `cat_pose/video_infer_save.py` | 播放影片時手動按鍵存幀，簡易版訓練圖片擷取工具 | 改頂部 `MODEL_PATH`/`VIDEO_DIR`（讀取資料夾內所有影片）/`OUTPUT_DIR`/`IMG_NAME_FORMAT`，執行後播放中按 `S` 存目前畫面、`SPACE` 切換 Step/Auto 模式。輸出圖片依 `walk{i}.png` 格式自動接續編號 |
| `cat_pose/建立攝影伺服器.py` | 通用攝影機伺服器：讀取本機 Webcam（`SOURCE=0`）或 ESP32-CAM（`SOURCE="http://..."`），推送畫面到 Node-RED，可本地錄影 | 改頂部 `SOURCE`（攝影機 index 或 ESP32-CAM URL）、`NODE_RED_URL`、`ENABLE_NODE_RED`，執行 `python 建立攝影伺服器.py`；視窗內按鈕或 `R` 鍵切換錄影、`Q` 結束。⚠️ 推送的 `/camera/frame` 端點目前不在現行 Node-RED 端點清單中，是否還在用建議先確認 |

`cat_pose/constants.py` 是共用視覺化常數模組（骨架連結定義、配色），非獨立腳本，供本節多支腳本（`0_video_pose_viewer.py`／`pose_single_image_test.py`／`eda_realtime_anomaly_viewer.py`／`tail_bend_detect.py` 等）與下一節的 `auto_labeling_capture.py` 共用 import。

---

## 2. cat_pose/自動標註工具/

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `cat_pose/自動標註工具/labeling.py` | tkinter 手動關鍵點標註工具 | 執行 `python labeling.py`，介面內選圖片/資料夾。⚠️ **關鍵點命名與現行系統不相容**：雖然同樣是 17 點，但用的是舊схема（`left_eye`/`right_eye`/`neck`/`body_mid`/`tail_base`…），現行 `constants.py` 已改用耳尖點等不同定義，拿這支標新資料會跟現在的模型/流程對不上 |
| `cat_pose/自動標註工具/auto_labeling.py` | 無監督自動標註：YOLO 推論 + 特徵聚類（KMeans/DBSCAN/PCA/t-SNE/IsolationForest）輔助批次產生訓練資料集 | 執行 `python auto_labeling.py`，tkinter 介面操作；內部關鍵點索引/骨架連結定義（17 點）跟現行 schema 一致，但依賴較重的 sklearn/matplotlib/seaborn 套件 |
| `cat_pose/自動標註工具/auto_labeling_capture.py` | 互動式影片標註：播放中按空白鍵凍結畫面編輯關鍵點，按 S 存檔繼續播放——**這支是相容於現行 `constants.py` schema 的版本**，三支裡建議優先用這支 | 改底部 `MODEL_PATH`/`VIDEO_PATH`/`OUTPUT_DIR`，執行 `python auto_labeling_capture.py`；按鍵：`SPACE`=凍結編輯、`S`=存檔並繼續、`A/D`=切換選取關鍵點、`W`=切換可見度、`0/1/2`=設定可見度等級、`+/-`=縮放、`Q`/`ESC`=結束。輸出 `images/`/`labels/`/`yolo_infer_images/` 到 `OUTPUT_DIR` |

---

## 3. cat_pose/tello_drone_archive/ ⚠️ 已停用

> 這 3 支是 DJI Tello 空拍機影像擷取/控制方案，現行系統的影像來源改用 `config.py` 的 `ModelPaths.VIDEO_INPUT`（本機路徑／攝影機索引／RTSP／ESP32-CAM URL，透過 `settings_window.py` 的「影像來源」多模式欄位管理），**沒有空拍機這個選項**。這批已於近期整理搬進本資料夾保留，僅供未來若要重新支援空拍機來源時參考，目前不在使用中。

| 路徑 | 功能 |
|---|---|
| `cat_pose/tello_drone_archive/tello_cat_pose.py` | Tello 空拍機即時姿態偵測 |
| `cat_pose/tello_drone_archive/tello_flask.py` | Tello 影像串流 Flask 伺服器 |
| `cat_pose/tello_drone_archive/tello_keyboard_control.py` | Tello 鍵盤飛行控制 |

---

## 4. paper/ 頂層

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `paper/1110.py` | ⚠️ **疑似跟本專案無關，建議確認去留**——VFX 去背景棋盤格轉透明工具，操作對象是 `C:\VFX\source`，跟貓咪監測系統完全無關聯，可能是誤放進本資料夾的檔案 | （若確認要留）改頂部路徑常數後執行 `python 1110.py` |
| `paper/1_youtube.py` | YouTube 貓咪影片資料集下載工具，支援增量更新（追新影片）+ 歷史回填（指定區間補下載舊影片） | 改頂部 `DOWNLOAD_DIR`/`CHANNEL_URL`/`FFMPEG` 路徑，`BACKFILL_START`/`BACKFILL_END` 設定回填區間，執行 `python 1_youtube.py`。需要 `yt_dlp`；下載記錄靠 `download_archive` 自動去重，重複執行安全 |
| `paper/config.py` | 全系統設定的單一事實來源（模型路徑/YOLO/ST-GCN 推論/Flask/Node-RED/行為追蹤閾值等）；`python config.py` 直接執行會印出完整配置摘要 + 驗證 + 自動同步 `default_runtime_settings.json` 跟目前硬編碼預設值一致 | `python config.py`。完整欄位對照表見 `paper/docs/設定視窗欄位對照表.md` |
| `paper/settings_window.py` | 設定管理 GUI（tkinter），管理 `runtime_settings.current.json` 執行期覆寫層，可視覺化編輯/驗證/匯入匯出設定，並可從這裡啟動/關閉 `main.py` | `python settings_window.py`。完整欄位/操作說明見 `paper/docs/設定視窗欄位對照表.md` |

---

## 5. paper/cat_monitoring_system/tools/

> 訓練/評估/資料整理用的離線工具，**不會**被 `main.py` 啟動的 Flask 服務呼叫。除下表列出者，`_smoothing_eval_common.py` 是共用函式庫（非獨立腳本），不列入。

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `0_train_gcn.py` | ST-GCN 訓練腳本主體 | 讀取 `stgcn_config.yaml`（可用環境變數 `STGCN_CONFIG_PATH` 覆寫路徑），執行 `python 0_train_gcn.py`。輸入來源為 `./data/skeletons/` 骨架 JSON，輸出訓練好的模型 |
| `1_classify_and_sort_videos.py` | 批次用 YOLO+ST-GCN 對整支影片逐幀分類，取多數類別把影片搬進對應行為資料夾 | 改頂部 `SOURCE_FOLDER`/`DEST_BASE`/`YOLO_MODEL_PATH`/`STGCN_MODEL_PATH`，執行 `python 1_classify_and_sort_videos.py`（背景批次，無視窗） |
| `1_export_keypoint_timeseries.py` | 匯出整段影片的關鍵點時序：CSV（逐幀座標/信心值）+ NPZ（原始/EMA平滑資料）+ Markdown 摘要 | 改頂部 `VIDEO_PATHS`/`YOLO_MODEL_PATH`/`MAX_VIDEOS_TO_PROCESS`，執行 `python 1_export_keypoint_timeseries.py` |
| `1_heic_av_png.py` | 圖片格式轉換工具（MODE 1：HEIC/HEIF→PNG，轉檔後刪原檔；MODE 2：PNG 右旋轉 90 度另存） | 改頂部 `MODE`（1 或 2）與 `ROOT_FOLDER`，執行 `python 1_heic_av_png.py`。⚠️ 原有 MODE 3（YOLO Pose 資料增強）已於近期整理移除，功能已由 `augmentation_yolo_pose.py` 取代，現在只剩兩種模式 |
| `1_measure_ear_distance_single_video.py` | 多影片左右耳距離監測，即時顯示歐式距離並寫入 CSV，內建簡易 HTTP server 可跨裝置查詢 | 改頂部影片清單設定，執行 `python 1_measure_ear_distance_single_video.py` |
| `1_run_video_inference.py` | 單支/多支影片離線推論（YOLO+ST-GCN+EMA 平滑），互動視窗版 | 改頂部 `VIDEO_PATHS`/`YOLO_MODEL_PATH`/`STGCN_MODEL_PATH`，執行 `python 1_run_video_inference.py`。⚠️ docstring 提及「與 test_video_inference.py 功能相同」，該檔名在專案中已不存在，純屬過期註解不影響本腳本可執行性 |
| `1_skeleton_visualizer.py` | 純骨架偵測與視覺化（不含接觸區域/行為統計/CSV），播放控制完整 | 改頂部 `VIDEO_LIST`/`VIDEO_PATH`，執行 `python 1_skeleton_visualizer.py` |
| `1_visualize_activity_score.py` | 用合成位移資料展示 Activity Score 機制對不同運動模式的反應（複製 anomaly_detector+behavior_tracker 算法） | 直接執行 `python 1_visualize_activity_score.py`，無需外部影片/模型；輸出同目錄 `activity_score_visualization.png` |
| `1_visualize_interpolation.py` | 互動視覺化 `interpolate_missing()` 補點效果：左右分割畫面比對「YOLO 原始偵測」vs「時間軸插值後」，可用滑鼠即時畫遮蔽區塊測試不同遮擋長度 | 執行 `python 1_visualize_interpolation.py`；按鍵：`space`=暫停/繼續、`a/d`=逐幀瀏覽、暫停時按 `o` 進入畫遮蔽區模式（左側面板連續點 4 下滑鼠組成遮蔽多邊形）、`s`=套用、`x`=清除 |
| `1_visualize_three_normalizations.py` | 互動 Demo：對照 `flip_normalize`／`orientation_normalize`／`normalize_skeleton_coords` 三種正規化步驟的視覺效果 | 執行後播放中按 `f`/`o`/`n` 個別切換三種正規化、`m` 切換置中基準、`p` 切換 raw/正規化畫面 |
| `1_多重命名.py` | tkinter GUI 影片觀看+批次改名工具 | 執行 `python 1_多重命名.py`，介面操作；改名後影片自動移入 `已改名/` 子資料夾 |
| `1_自動抓取.py` | 從 Pixabay/Pexels API 依關鍵字批次搜尋+下載免費貓咪行為影片素材 | 需先申請兩個平台的免費 API key（設環境變數 `PIXABAY_API_KEY`/`PEXELS_API_KEY` 或直接改程式），執行 `python 1_自動抓取.py`。輸出 `cat_behavior_videos.csv` + `downloaded_videos/<時間戳記>/`，執行紀錄累積在 `run_log.csv` |
| `2_run_dual_model_compare.py` | 雙模型即時並排比較：同一影片、左右兩視窗分別跑獨立的 YOLO+ST-GCN pipeline，肉眼比較骨架穩定度與行為判斷差異 | 改頂部模型 A/B 路徑，執行 `python 2_run_dual_model_compare.py`。純視覺比較，量化比較請改用 `eval_pose_compare.py`/`eval_gcn_compare.py` |
| `3_cat_identity_verification_test.py` | 貓咪個體身分辨識測試（顏色特徵直方圖 + 兩類最近鄰分類，非關鍵點幾何），驗證多貓同框時的身分區分可行性 | 改頂部設定區的 `CAT_PROFILES`（各貓的參考影片）與 `RUN_MODE`，執行 `python 3_cat_identity_verification_test.py`；`RUN_MODE="enroll"` 建基準、`"verify"` 驗證。`SHOW_PREVIEW=True` 時按鍵：`q`=退出、`space`=暫停、`1/2`=切換影片。輸出逐幀判定 CSV |
| `eval_class_source_distribution.py` | 檢查各行為類別訓練樣本是否集中於少數影片/場景（過擬合假象排查） | `python eval_class_source_distribution.py`，或帶參數 `--skeleton_dir <path> --top_n 5` |
| `eval_ema_ablation.py` | 比較不同關鍵點 EMA alpha 訓練出的 ST-GCN 模型表現 | 改頂部 `MODELS` 清單與 `HARD_OUTPUT_DIR`，執行 `python eval_ema_ablation.py`。輸出 `<HARD_OUTPUT_DIR>/ema_ablation_NNN/`（準確率長條圖/混淆矩陣/機率直方圖/summary CSV） |
| `eval_gcn_compare.py` | 2~5 個 ST-GCN 模型在多支標註影片上的量化比較（離散準確率/平均信心/事件偵測率/Macro F1/混淆矩陣） | CLI：`python eval_gcn_compare.py --models <path1> <path2> ... --names <name1> <name2> ...`，另有 `--ema_alphas`/`--seq_lens`/`--video_walk_dir` 等參數可覆寫預設路徑。輸出 `<output>/comparison_NNN_.../` |
| `eval_lighting_distribution.py` | 抽樣訓練影片幾幀算 HSV 亮度均值，統計各行為類別光照分布，找出最暗/最亮影片供人工抽查 | `python eval_lighting_distribution.py`，或 `--n_samples 8 --dark_threshold 60` |
| `eval_model_worst_videos.py` | 單一 ST-GCN 模型在全部行為類別影片上逐支評分，找出表現最差的影片供人工檢視/重新標註 | CLI 執行，`--help` 查看完整參數；完全背景執行（matplotlib Agg），輸出 `<output>/single_eval_NNN_<name>/`（video_breakdown.csv/per_class_accuracy.png/confusion_matrix.png） |
| `eval_pose_compare.py` | 兩個 YOLO Pose 模型在標準測試集上的量化比較 benchmark，完全背景執行 | 改頂部 `BENCHMARK_WALK_DIR` 等五個類別資料夾路徑，執行 `python eval_pose_compare.py`。輸出 CSV/PNG/TXT/LOG/HTML 完整報告 |
| `run_keypoint_trend_from_videos.py` | 直接對某資料夾內同一類別的原始影片重新推論，檢視單一行為類別各關鍵點動作幅度與跨影片趨勢一致性（不依賴既有訓練資料集） | `python run_keypoint_trend_from_videos.py`，或 `--video_folder "<path>" --class_name scratch`（不給則類別名稱自動取資料夾名）。⚠️ docstring 提及對照腳本 `run_keypoint_verification.py`，該檔在專案中已不存在，屬過期註解 |
| `test_anomaly_detection.py` | 靜止偵測測試（模式 1：背景批次輸出 CSV；模式 2：GUI 互動視窗，可即時調整門檻） | 執行 `python test_anomaly_detection.py`，GUI 模式按鍵：`q/ESC`=結束、`Space`=暫停、`1/2`=切換影片；終端可輸入 `motion <值>` 或 `mean <整數>` 後按 `s` 套用新門檻 |
| `test_bbox_area_ratio.py` | BBox 佔畫面比例量測（模式 1：即時影片/攝影機顯示；模式 2：資料夾批次依佔比分類搬移圖片） | 改頂部 `RUN_MODE`（0=互動選擇/1/2）與模式 2 專用的 `MODE2_FOLDER`/`MODE2_OUTPUT_DIR`，執行 `python test_bbox_area_ratio.py` |
| `test_bone_length_stability.py` | 骨架穩定度診斷/校準工具（骨段長度一致性/脊椎中點偏移角度/Body Axis jitter），用來校準 SQA（Skeleton Quality Assessment）機制的門檻值 | 改頂部 `RUN_MODE`（留 `None` 執行時互動選單輸入 1/2/3），支援多影片（1~10支）依序處理再疊圖比較，執行 `python test_bone_length_stability.py` |
| `test_pose_jitter_analysis.py` | 姿勢抖動分析（4 種模式：BBox 穩定性／關鍵點穩定性／關鍵點信心度相關性／BBox EMA 平滑分析），單支影片版 | 改頂部 `ANALYSIS_MODE`，執行 `python test_pose_jitter_analysis.py`；播放中按 `q` 提前結束 |
| `影片拼接.py` | 合併 Tapo C200 監視器影片片段（依時間順序，模式 1 直接串接／模式 2 補黑畫面填補時間空隙） | 改頂部 `INPUT_FOLDER`/`OUTPUT_NAME`/`MODE`（1 或 2），執行 `python 影片拼接.py`。需系統已安裝 ffmpeg/ffprobe |

---

## 6. paper/cat_monitoring_system/tools/train_data/

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `0_dataset_collect.py` | 骨架資料集收集與手動標注主力工具，7 種執行模式 | 執行 `python 0_dataset_collect.py`，終端選單輸入 1~7：**1**=批次推論訓練影片（增量、依資料夾名整段標記）／**2**=連續手動標記事件區間／**3**=換新模型後重新推論骨架、依 timestamp 還原既有標籤／**4**=純讀取報告，檢查哪些資料被捨棄／**5**=同模式3但只針對指定部分檔案／**6**=獨立測試集標註（先選要抽骨架/標註/兩者都做）／**7**=純讀取，印出測試集標註報告表格+輸出 CSV。模式 1-5 用 `VIDEO_FOLDERS`/`OUTPUT_FOLDER`，模式 6/7 用完全獨立的 `TEST_VIDEO_FOLDERS`/`TEST_OUTPUT_FOLDER`，兩者不會混淆 |

---

## 7. paper/_tools/

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `1_read_context.py` | 唯讀診斷工具：印出 Node-RED `global.json`（`NodeRedConfig.GLOBAL_CONTEXT_PATH`，預設 `C:\a\global.json`）目前累積的個體化基線資料（每日監測記錄/排除清單/基線快照），不需任何服務在跑 | `python _tools/1_read_context.py`。只讀不寫，執行多少次都安全 |
| `2_backfill_daily_store.py` | 一次性（但冪等可重跑）搬遷工具：把 Node-RED `global.json` 的 `v2_daily_history` 灌進 Python 獨立的 `analytics/daily_store.py`（SQLite），讓 `/dashboard/baseline` 不用等 7 天真實時間累積才有基線可看 | `python _tools/2_backfill_daily_store.py`。同一天重複跑會被 upsert 覆蓋，不會重複寫入 |

---

## 8. paper/node_red_tests/

| 路徑 | 功能 | 使用方法 |
|---|---|---|
| `fake_history_gen.py` | 產生「個體化基線」用的假每日彙整資料（情境化、可調參數），供 UI 預覽頁面測試用 | 改頂部 `SCENARIOS` 情境參數，執行 `python fake_history_gen.py`，產生同目錄 `fake_scenarios.json`，接著跑 `node render_baseline_ui_preview.js` 產生預覽頁面 |
| `fetch_real_history.py` | 唯讀抓取 Node-RED 目前真實累積的 `v2_daily_history`，存成快照供 UI 預覽頁面「複製真實資料再竄改」模式使用 | `python fetch_real_history.py`，產生同目錄 `real_history_snapshot.json`。⚠️ 全程唯讀，不會寫回 `C:\a\global.json` |

---

## 📎 延伸閱讀

- `paper/docs/0_AI_專案導覽地圖.md` 第 3.6 節 — `cat_monitoring_system/tools/` 的簡短分類總覽
- `paper/docs/設定視窗欄位對照表.md` — `config.py`／`settings_window.py`／`settings_manager.py` 完整欄位說明
