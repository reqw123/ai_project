# 測試操作手冊

給第一次接觸這套測試的你看的操作指南。這份文件只講「怎麼用」；設計理念與函式分類請看 [0_測試策略分析.md](0_測試策略分析.md)。

---

<a id="sec-1"></a>
## 一、先搞懂幾個名詞

- **單元測試 (Unit Test)**：針對一個函式，給固定輸入，斷言輸出等於「你手算/推導出來」的正確答案。適合邏輯簡單、可以人工驗證的函式（純函式）。斷言的是「這個答案本來就該是這樣」，不是「反正現在輸出這樣」。
- **特徵化測試 / 回歸測試 (Characterization Test / Regression Test)**：適合邏輯太複雜、牽涉真實模型/GPU，沒辦法手算正確答案的情況。做法是：先把「目前系統對某個固定輸入的實際輸出」凍結成一份**快照 (Snapshot)** 檔案，之後每次跑測試，都拿「現在的輸出」跟這份快照比對。
  - 這種測試**不保證輸出是對的**，只保證「沒有在你不知情的狀況下被改變」。
  - 好處：可以幫還沒有測試保護的舊程式碼（例如 `FrameProcessor.process()`）先建立一張安全網，之後才敢放心重構。
- **API Regression Test**：針對 Flask 路由（`server/routes.py`），驗證的是「API 的對外契約」——HTTP 狀態碼、回應 JSON 有哪些欄位（schema/keys）、錯誤處理是否符合預期。**刻意不驗證任何演算法/推論結果的正確性**（那些已經在上面兩種測試涵蓋過），只確保未來重構路由內部實作時，前端/Node-RED 呼叫這些 API 的方式不會被悄悄破壞。
- **快照 (Snapshot)**：一份 JSON 檔案，記錄「基準行為」。只有 Regression/Characterization Test 會用到快照；Unit Test 與 API Regression Test 都是直接斷言明確的期望值/schema，不用快照。測試時只負責「比對」，**不會自動幫你重新產生**——這是刻意設計，避免快照被不小心刪除後，測試自動重建、永遠通過、卻失去了偵測問題的能力。

---

<a id="sec-2"></a>
## 二、目前有哪些測試

**總計 428 個測試**（`yolo_new` 環境全數執行；base 環境可執行 170 個，其餘 258 個因為需要 cv2/torch/flask 而 skip）。

> **2026-08-11 更新**：以上是本節（2.1~2.4）當初建置完成時的快照，之後陸續新增的測試見下方「[2.5 之後陸續新增的測試](#sec-2-5)」——**目前實測總計 487 個測試**（`python -m pytest cat_monitoring_system tests --collect-only -q` 核對過）；base 環境（沒有 cv2/torch/flask 的一般 conda base）目前實測可執行 **194 個**（跑過確認全過，見 [2.5 節](#sec-2-5)說明是哪些檔案），其餘 293 個要 `yolo_new` 環境。

<a id="sec-2-1"></a>
### 2.1 Phase 1 — Characterization / Regression Test

| 資料夾 / 檔案 | 測試對象 | 測試數 | 需要 GPU/真實模型？ |
|---|---|---|---|
| `trackers/tests/test_behavior_tracker_regression.py` | `ImprovedBehaviorTracker`（行為切換/duration/count/transition/alert/存讀） | 9 | 否（用假時鐘控制） |
| `processors/tests/test_frame_processor_characterization.py` | `FrameProcessor.process()`（單一 walk 影片） | 2 | **是**（真實 YOLO + ST-GCN） |
| `processors/tests/test_golden_dataset.py` | 5 種行為（walk/lick/scratch/shake/stop）的 prediction/statistics/Node-RED payload/CSV/行為區段 | 25 | **是**（真實 YOLO + ST-GCN） |

<a id="sec-2-2"></a>
### 2.2 Phase 2 — Unit Test（依 Pure Function 優先順序）

| 優先序 | 資料夾 / 檔案 | 測試對象 | 測試數 |
|---|---|---|---|
| 1 | `processors/tests/test_skeleton_quality_assessment_unit.py` | `processors/skeleton_quality_assessment.py` 全部函式（骨架品質雙重判定） | 35 |
| 2 | `processors/tests/test_stgcn_preprocessing_unit.py` | `models/stgcn_model.py` 前處理鏈（`interpolate_missing`→…→`build_feature_tensor`） | 29 |
| 3 | `paper/tests/test_config_unit.py` | `config.py`：`RunModeConfig`、環境變數解析函式們 | 81 |
| 4 | `server/tests/test_routes_pure_functions_unit.py` | `server/routes.py`：`_daily_record_from_dict()`、`_dataclass_to_jsonable()` | 10 |
| 5 | `trackers/tests/test_behavior_tracker_pure_functions_unit.py` | `BehaviorTracker` 的純狀態函式（`map_gcn_to_tracker`/`get_alerts`/`_settle_current_behavior`） | 22 |
| 6 | `utils/tests/test_helpers_unit.py` | `utils/helpers.py` 全部純函式 | 39 |
| 7 | `plugins/tests/test_contact_regions_unit.py` | `plugins/lick_stage/contact_regions.py` 幾何函式 | 38 |
| 7 | `plugins/tests/test_head_direction_unit.py` | `plugins/lick_stage/head_direction.py` 頭部朝向推論/平滑函式 | 34 |
| 7 | `plugins/tests/test_ear_distance_unit.py` | `plugins/lick_stage/ear_distance.py::compute_ear_distance()` | 7 |
| 7 | `plugins/tests/test_ext_body_zones_regions_unit.py` | `plugins/lick_stage/ext_body_zones/regions.py` 7 區身體分區幾何函式 | 42 |

Phase 2 小計：337 個。

<a id="sec-2-3"></a>
### 2.3 API Regression Test

| 資料夾 / 檔案 | 測試對象 | 測試數 |
|---|---|---|
| `server/tests/test_routes_api_regression.py` | `server/routes.py` 全部 7 個 Flask 路由（`/`、`/stream`、`/snapshot`、`/video_clip`、`/api/behavior_history`、`/api/deviation`、`/api/overlay`） | 36 |

用 Flask test client + 假的 `frame_processor`/`frame_streamer` 物件跑，不需要真實攝影機/GPU/Node-RED。

<a id="sec-2-4"></a>
### 2.4 更早就有的測試

| 資料夾 | 測試對象 | 測試數 |
|---|---|---|
| `analytics/tests/` | `analytics/` 個體基線/偏差/融合的純函式 | 19 |

<a id="sec-2-5"></a>
### 2.5 之後陸續新增的測試（2026-08-11 補記，非當初 Q6/Q7 建置範圍）

上面 2.1~2.4 是這份文件當初完成建置時的狀態，之後功能陸續增加，各自帶了自己的測試，沒有回頭補進上面的表格（避免打亂「當初分類判斷」的歷史紀錄），統一記在這裡：

| 資料夾 / 檔案 | 測試對象 | 測試數 | 需要 GPU/torch/cv2？ |
|---|---|---|---|
| `analytics/tests/test_daily_store.py` | `analytics/daily_store.py`（SQLite 多天歷史持久化） | 13 | 否 |
| `analytics/tests/test_config.py` | `analytics/config.py`（`BaselineConfig` 環境變數解析） | 9 | 否 |
| `models/tests/test_keypoint_kalman.py` | `models/keypoint_kalman.py`（逐關鍵點 Kalman filter，純合成軌跡驗證） | 7 | 否（純 numpy） |
| `plugins/tests/test_nodered_publisher_unit.py` | `plugins/lick_stage/publisher.py`（非阻塞 HTTP 推送、限流警告） | 4 | 是（透過 import chain 間接需要 cv2） |
| `dashboard/tests/test_cache.py` | `dashboard/cache.py`（記憶體快取層） | 5 | 是 |
| `dashboard/tests/test_refresher.py` | `dashboard/refresher.py`（背景排程刷新、`_cache_is_fresh()`） | 7 | 是 |
| `dashboard/tests/test_views.py` | `dashboard/views.py` | 4 | 是 |
| `dashboard/tests/test_flag_integration.py` | `BaselineDashboardConfig.ENABLED` 旗標整合 | 2 | 是 |

另外兩個既有檔案也累積了新測試案例，數字比 2.1/2.3 表格記錄的當初數字多：
- `trackers/tests/test_behavior_tracker_regression.py`：[2.1 節](#sec-2-1)記錄 9 個，**目前 11 個**（新增每日重置持久化相關案例）。
- `server/tests/test_routes_api_regression.py`：[2.3 節](#sec-2-3)記錄 36 個，**目前 42 個**（`/api/deviation` 欄位可省略後新增的預設值行為測試）。
- `analytics/tests/`：[2.4 節](#sec-2-4)記錄 19 個，加上這裡新增的 `test_daily_store.py`+`test_config.py`，**目前共 41 個**。

`dashboard/tests/*` 四個檔案雖然本身邏輯跟 cv2 無關，但 `dashboard/refresher.py`/測試檔透過 import chain 間接需要 `server.routes`（有 cv2 依賴），所以歸在需要完整環境才能跑的那一類，不在 [2.5 節](#sec-2-5)的「否」清單裡。⚠️ **2026-09-04 補充**：`dashboard/tests` 目前**沒有**登記在 `pyproject.toml` 的 `testpaths` 裡，裸執行 `pytest` 不會跑到它，細節見 [2.6 節](#sec-2-6)與 [6.3 節](#sec-6-3)。

<a id="sec-2-6"></a>
### 2.6 2026-09-04 核對更新（環境更名 + 新增測試檔 + 完整計數）

跟 [2.5 節](#sec-2-5)一樣，這裡只記錄跟原文有出入、需要更正的地方，不回頭改上面的歷史數字。

- 🚨 **`yolo_new` 環境已不存在**：本機 `conda env list` 目前只有 `yolo`（沒有 `yolo_new`）。無法確認是「環境改名」還是文件從頭就寫錯，但實測目前能用的名稱就是 `yolo`——本文件第三、四節的指令已全部改成 `conda activate yolo`。
- 🚨 **`pytest.ini` 已改放到 `pyproject.toml`**：詳見 [6.3 節](#sec-6-3)。
- **`paper/tests/` 資料夾之後新增了 3 個測試檔**，原文（第 2.2 節 Phase 2 P3）只記錄了 `test_config_unit.py`（81 個），之後陸續新增、未回頭補表格：
  - `test_settings_manager.py` — 76 個（`settings_manager.py` 驗證/原子存讀/匯入匯出/diff）
  - `test_ui_state_recent_video_paths.py` — 9 個
  - `test_ui_state_script_order.py` — 8 個
- **裸執行 `pytest`（`pyproject.toml` 登記的 8 個資料夾，含新增的 `models/tests`）2026-09-04 實測：570 個測試收集到，543 過、27 skip、0 失敗**。27 個 skip 全部是 [2.1 節](#sec-2-1) Phase 1 的 `test_frame_processor_characterization.py`（2 個）／`test_golden_dataset.py`（25 個），因為這台機器沒有固定測試影片/模型檔可跑，不算失敗——[2.5 節](#sec-2-5)記錄的「1 個已知既有失敗（`activity_value` GPU 浮點誤差）」目前已不存在，實測全過。
- **`dashboard/tests`（18 個，全過）沒有登記在 `testpaths` 裡**，裸執行 `pytest` 不會跑到，必須另外指定路徑：`pytest paper/cat_monitoring_system/dashboard/tests -v`。所以「全專案測試總數」視怎麼算而不同：裸執行看到的是 570，含 `dashboard/tests` 的話全部檔案總計 **588 個（561 過、27 skip、0 失敗）**。
- base 環境（無 cv2/torch）用「已知安全子集」（`analytics/tests`＋`trackers/tests`＋`utils/tests`＋`paper/tests`）實測 **291 個，全過**——比 [2.5 節](#sec-2-5)記錄的 194 多，因為 `paper/tests` 新增了上述 3 個檔案。⚠️ **裸執行 `pytest`（不給路徑）在 base 環境目前會直接收集失敗、無法啟動**（`plugins/tests/test_nodered_publisher_unit.py`、`models/tests/test_keypoint_kalman.py` 這兩個檔案沒有 `pytest.importorskip("cv2"/"torch", ...)` 保護，import chain 缺套件時是拋錯而不是優雅跳過），不是原文暗示的「優雅跳過」，base 環境請照第四節指定路徑執行。

---

<a id="sec-3"></a>
## 三、環境需求

專案裡有兩個會用到的 Python 環境：

- **base 環境**（你平常開終端機預設的那個）：沒裝 `torch`/`cv2`/`ultralytics`/`flask`。只能跑不需要這些套件的測試——`analytics/tests`（含 2.5 節新增的 `test_daily_store.py`/`test_config.py`）、`trackers/tests`、`utils/tests`、`paper/tests`，**2026-08-11 當時實測共 194 個，全過**（原文寫的 170 是當初建置完成時的數字，之後這幾個資料夾也新增了測試；**2026-09-04 因 `paper/tests` 新增 3 個檔案，實測已變成 291 個，全過**，見 [2.6 節](#sec-2-6)）。
- **`yolo` conda 環境**（🚨 **2026-09-04 更正**：原文一直寫的 `yolo_new` 是錯的／過時的環境名稱，本機沒有這個環境，實際名稱是 `yolo`）：裝了完整的 GPU/YOLO/ST-GCN/Flask 依賴，**跑全部測試都要用這個**。裸執行 `pytest` 目前實測 570 個測試（543 過、27 skip、0 失敗）；`dashboard/tests`（18 個，全過）未登記在掃描範圍內，要另外指定路徑（見 [6.3](#sec-6-3)）。原文寫的 487／428 已過時，完整說明見 [2.6 節](#sec-2-6)。

```powershell
conda activate yolo
```

不確定現在是哪個環境，看提示字元前面有沒有 `(yolo)`。

⚠️ **有些「明明是純函式、不用 GPU」的模組，在 base 環境還是會被整批跳過（或直接收集失敗）**（例如 `plugins/lick_stage/` 底下的幾何函式、`server/routes.py` 裡的 `_daily_record_from_dict()`）。這不是測試寫錯，是因為這些檔案的 import chain 會間接拉到 cv2/torch/flask（細節見第六節 [6.5](#sec-6-5)）。

---

<a id="sec-4"></a>
## 四、常用指令

**你習慣待在 `C:\ai_project` 開發，以下指令都假設你在那個目錄。**

<a id="sec-4-1"></a>
### 4.1 跑全部測試

```powershell
cd C:\ai_project
conda activate yolo
python -m pytest -v
```

裸執行 `pytest`（不給路徑）會自動只掃描 `pytest.ini` 裡登記的測試資料夾，不會誤觸專案裡其他無關的腳本。

<a id="sec-4-2"></a>
### 4.2 只跑不需要 GPU 的測試（隨便什麼環境都行，速度快）

```powershell
python -m pytest paper/cat_monitoring_system/analytics/tests paper/cat_monitoring_system/trackers/tests paper/cat_monitoring_system/utils/tests paper/tests -v
```

<a id="sec-4-3"></a>
### 4.3 只跑某一個模組 / 某一個階段

```powershell
# 只跑 Phase 1（Characterization / Regression Test）
python -m pytest paper/cat_monitoring_system/trackers/tests/test_behavior_tracker_regression.py paper/cat_monitoring_system/processors/tests/test_frame_processor_characterization.py paper/cat_monitoring_system/processors/tests/test_golden_dataset.py -v

# 只跑 Phase 2（Unit Test）某個優先序項目，例如 plugins 幾何函式
python -m pytest paper/cat_monitoring_system/plugins/tests -v

# 只跑 API Regression Test
python -m pytest paper/cat_monitoring_system/server/tests/test_routes_api_regression.py -v
```

<a id="sec-4-4"></a>
### 4.4 只跑名稱包含某個關鍵字的測試

```powershell
python -m pytest -k "scratch" -v          # 只跑名稱或參數含 "scratch" 的測試
python -m pytest -k "alerts" -v           # 只跑警報相關的測試
python -m pytest -k "overlay" -v          # 只跑 /api/overlay 相關的測試
```

<a id="sec-4-5"></a>
### 4.5 其他好用參數

```powershell
python -m pytest -v -s              # -s：顯示 print 輸出（例如模型載入訊息）
python -m pytest --collect-only -q  # 只列出有哪些測試，不執行
python -m pytest -x                 # 遇到第一個失敗就停止，不繼續跑後面的
python -m pytest --tb=short         # 測試失敗時，錯誤訊息只顯示精簡版
```

---

<a id="sec-5"></a>
## 五、快照怎麼用

**只有 Phase 1（Characterization / Regression Test）才有快照**；Unit Test 跟 API Regression Test 都是直接斷言明確答案/schema，沒有快照檔案要管理。

<a id="sec-5-1"></a>
### 5.1 平常跑測試 —— 不用管快照

`python -m pytest ...` 只會**讀取**既有快照做比較，不會自己產生或修改快照檔案。

<a id="sec-5-2"></a>
### 5.2 什麼時候需要手動更新快照

只有在你**主動確認**「現在的行為就是我要保留的新基準」時，才手動執行對應的產生腳本，例如：

- 換了一版 YOLO 或 ST-GCN 模型
- 調整了 `SQAConfig` 或其他門檻值，預期行為本來就該不一樣
- 重構了 `process()`/`BehaviorTracker` 內部邏輯，肉眼確認過新輸出仍然合理

```powershell
# 更新 FrameProcessor characterization 快照
python paper/cat_monitoring_system/processors/tests/generate_frame_processor_snapshot.py

# 更新 BehaviorTracker 快照
python paper/cat_monitoring_system/trackers/tests/generate_behavior_tracker_snapshot.py

# 更新 Golden Dataset 快照（5 種行為）
python paper/cat_monitoring_system/processors/tests/generate_golden_dataset_snapshot.py
```

⚠️ **如果測試失敗，不要直接重跑產生腳本讓測試變綠**。應該先搞清楚「為什麼跑掉」，確認是上面列的那幾種預期中的改動，才手動更新快照——否則等於把可能的 bug 直接吃進基準裡，測試就永久失去偵測那個問題的能力了。

<a id="sec-5-3"></a>
### 5.3 快照檔案放在哪裡

```
trackers/tests/snapshots/behavior_tracker_scripted_scenario.json
processors/tests/snapshots/frame_processor_process_characterization.json
processors/tests/snapshots/golden_dataset/{walk,lick,scratch,shake,stop}.json
```

這些檔案要跟程式碼一起進版控（git add），這樣其他人（或未來的你）才有基準可以比對。

---

<a id="sec-6"></a>
## 六、常見狀況

<a id="sec-6-1"></a>
### 6.1 測試被 SKIPPED，不是 PASSED/FAILED

`processors/tests` 裡的 Phase 1 測試需要真實的 YOLO/ST-GCN 模型檔跟固定測試影片存在才會執行，找不到就會顯示 `skipped`，不算失敗。在沒裝 cv2/torch/flask 的環境底下，所有需要這些套件才能 import 的測試檔也會直接整批 skip，不會報錯中斷（細節見 [6.5](#sec-6-5)）。

<a id="sec-6-2"></a>
### 6.2 GPU 浮點數比對「差一點點」也算過

`confidence`/`class_probs` 這類 ST-GCN 推論輸出，用的是容許誤差比較（目前設定 `1e-4`），不是要求完全相等——不同硬體/驅動版本跑出來的浮點數本來就會有極小差異，這是正常現象，不算回歸。`behavior_id`/`is_still`/次數這類離散值才要求完全相等。

<a id="sec-6-3"></a>
### 6.3 為什麼有 `pytest.ini`

> 🚨 **2026-09-04 更新**：`pytest.ini` 已於 2026-08-29（見 `TODO.md` M-1）搬到 **`C:\ai_project\pyproject.toml` 的 `[tool.pytest.ini_options]`**——原檔含中文註解，在部分環境（Windows `cp950` locale）下 `iniconfig` 讀檔會直接 `UnicodeDecodeError`、pytest 完全無法啟動；`pyproject.toml` 固定以 UTF-8 讀取不受影響。本節標題沿用舊檔名方便查找，內容/功能不變，只是換了個檔案放。

`pyproject.toml` 的 `testpaths` 限定了裸執行 `pytest`（沒給路徑）時的預設掃描範圍，避免不小心掃到專案裡其他無關的腳本（例如 `cat_pose/` 底下一支檔名剛好符合測試命名規則、但其實會跳出互動視窗的手動除錯腳本）。明確指定路徑執行時不受這個限制。目前登記的資料夾（**2026-09-04 核對，比原文多了 `models/tests`**）：

```
paper/cat_monitoring_system/analytics/tests
paper/cat_monitoring_system/processors/tests
paper/cat_monitoring_system/trackers/tests
paper/cat_monitoring_system/server/tests
paper/cat_monitoring_system/utils/tests
paper/cat_monitoring_system/plugins/tests
paper/cat_monitoring_system/models/tests
paper/tests
```

⚠️ **`dashboard/tests`（18 個測試）不在上面清單內**，裸執行 `pytest` 不會跑到它，要另外指定路徑：`pytest paper/cat_monitoring_system/dashboard/tests -v`。

<a id="sec-6-4"></a>
### 6.4 測試會不會動到正式的 CSV/tracker 狀態檔？

不會。每個測試都用 `monkeypatch`／假時鐘把 `config.py::LoggingConfig`（或 `ModelPaths.OUTPUT_DIR` 等）的路徑導向暫時的測試資料夾，不會碰到正式的 `cat_monitoring_log.csv`、`behavior_segments_log.csv`、`tracker_state.json`、`ext_body_zones/results.csv`，也不會連上 Node-RED 或發送任何網路請求。

<a id="sec-6-5"></a>
### 6.5 為什麼「明明是純函式」的模組也需要 yolo 環境才能收集

Python 的 import 是「整個模組檔案」層級的，不是「單一函式」層級的。以下幾種情況會讓一個本身零 GPU/cv2 邏輯的函式，還是需要對應套件才能被 pytest 收集：

- `server/routes.py` 檔案**開頭**就 `import cv2`、`from flask import ...`、`from processors.frame_processor import FrameProcessor`（間接需要 torch）——即使只是要測試檔案裡的 `_daily_record_from_dict()` 這種純函式，import 這支檔案本身就需要完整環境。
- `plugins/lick_stage/__init__.py` 會 `from plugins.lick_stage.manager import LickStagePlugin`，`manager.py` 又 `import` 了需要 cv2 的 `overlay.py`。所以只要 import `plugins.lick_stage.*` 底下**任何**子模組（包括完全沒有 cv2 邏輯的 `contact_regions.py`/`head_direction.py`/`ear_distance.py`，以及它的子套件 `ext_body_zones/regions.py`），都會觸發這條 import 鏈。

這些測試檔案開頭都有 `pytest.importorskip("cv2", reason="...")`，在沒裝 cv2 的環境會整份檔案安全跳過收集（顯示 skipped），不會報錯中斷其他測試。

<a id="sec-6-6"></a>
### 6.6 API Regression Test 為什麼不怕碰到真的攝影機/GPU？

`server/tests/test_routes_api_regression.py` 建立獨立的 Flask app，並把 `server.routes` 模組層級的 `frame_processor`/`frame_streamer` 全域變數直接換成假物件（只實作路由邏輯實際會用到的屬性/方法），同時把 `_ensure_processor_started()` monkeypatch 成 no-op。這樣不管現在是不是在 `RunModeConfig` 的排程視窗內、有沒有接攝影機，測試行為都完全確定。

---

<a id="sec-7"></a>
## 七、完整檔案結構

> 🚨 **2026-09-04 更新**：`pytest.ini` 已改放到根目錄 `pyproject.toml`（見 [6.3 節](#sec-6-3)）；`paper\tests\` 新增了 3 個檔案；`cat_monitoring_system\` 下新增了 `models\tests\`（已登記進 `testpaths`）與 `dashboard\tests\`（**未登記**）。下面樹狀圖已更新反映目前實際檔案。

```
C:\ai_project\
├── pyproject.toml                                    # [tool.pytest.ini_options]，限定裸執行 pytest 的掃描範圍（原 pytest.ini）
└── paper\
    ├── tests\                                        # config.py / settings_manager.py / ui_state 的 Unit Test
    │   ├── conftest.py
    │   ├── test_config_unit.py
    │   ├── test_settings_manager.py                  # 2026-09-04 核對：新增，76 個測試
    │   ├── test_ui_state_recent_video_paths.py        # 2026-09-04 核對：新增，9 個測試
    │   └── test_ui_state_script_order.py              # 2026-09-04 核對：新增，8 個測試
    └── cat_monitoring_system\
        ├── analytics\tests\
        │   ├── conftest.py
        │   └── test_baseline.py / test_deviation.py / test_fusion.py / test_daily_store.py / test_config.py
        ├── trackers\tests\
        │   ├── conftest.py
        │   ├── _scenario_utils.py                    # 共用假時鐘 + 腳本化情境
        │   ├── generate_behavior_tracker_snapshot.py # 手動：更新快照
        │   ├── test_behavior_tracker_regression.py       # Phase 1
        │   ├── test_behavior_tracker_pure_functions_unit.py  # Phase 2 P5
        │   └── snapshots\behavior_tracker_scripted_scenario.json
        ├── processors\tests\
        │   ├── conftest.py
        │   ├── _snapshot_utils.py                    # FrameProcessor 測試共用邏輯
        │   ├── _golden_dataset_utils.py              # Golden Dataset 共用邏輯（GoldenClock 等）
        │   ├── generate_frame_processor_snapshot.py  # 手動：更新 FrameProcessor 快照
        │   ├── generate_golden_dataset_snapshot.py   # 手動：更新 Golden Dataset 快照
        │   ├── test_frame_processor_characterization.py     # Phase 1
        │   ├── test_golden_dataset.py                        # Phase 1
        │   ├── test_skeleton_quality_assessment_unit.py      # Phase 2 P1
        │   ├── test_stgcn_preprocessing_unit.py              # Phase 2 P2
        │   └── snapshots\
        │       ├── frame_processor_process_characterization.json
        │       └── golden_dataset\{walk,lick,scratch,shake,stop}.json
        ├── server\tests\
        │   ├── conftest.py
        │   ├── test_routes_pure_functions_unit.py    # Phase 2 P4
        │   └── test_routes_api_regression.py         # API Regression Test
        ├── models\tests\                             # 2026-09-04 核對：已登記進 testpaths
        │   ├── conftest.py
        │   └── test_keypoint_kalman.py               # 7 個測試，⚠️ 無 importorskip 保護（見 6.5）
        ├── utils\tests\
        │   ├── conftest.py
        │   └── test_helpers_unit.py                  # Phase 2 P6
        ├── plugins\tests\
        │   ├── conftest.py
        │   ├── test_contact_regions_unit.py           # Phase 2 P7
        │   ├── test_head_direction_unit.py             # Phase 2 P7
        │   ├── test_ear_distance_unit.py                # Phase 2 P7
        │   ├── test_ext_body_zones_regions_unit.py       # Phase 2 P7
        │   └── test_nodered_publisher_unit.py            # 見 2.5 節，⚠️ 無 importorskip 保護（見 6.5）
        └── dashboard\tests\                          # 2026-09-04 核對：⚠️ 未登記進 testpaths，需指定路徑執行
            ├── conftest.py
            ├── test_cache.py / test_refresher.py / test_views.py / test_flag_integration.py
```

檔名開頭是底線的（`_scenario_utils.py`、`_snapshot_utils.py`、`_golden_dataset_utils.py`）都不是測試本身，是共用工具模組；`generate_*.py` 都不是 pytest 測試，是手動更新快照用的腳本——這兩類都不會被 `pytest` 收集執行。
